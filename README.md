# Query on PDF

A notebook that lets you pass in any PDF and query it - the model retrieves the relevant answer to your question via similarity search on a vector store built from the document.

## Tech stack
Python, LangChain, FAISS/vector store, Jupyter Notebook
